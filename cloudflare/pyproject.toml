[project]
name = "agent-inboxes-cloudflare"
version = "0.1.0"
requires-python = ">=3.13"
dependencies = []

[dependency-groups]
dev = [
    "cryptography>=50.0.1",
    "uv>=0.12.3",
    "workers-py",
    "workers-runtime-sdk",
]
